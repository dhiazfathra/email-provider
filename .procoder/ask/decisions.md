## Next step after the S1 and S2 specs

The specs, ADRs and evidence scaffold are written but uncommitted. The
brainstorming flow's next step is the writing-plans skill, not implementation.

- **Commit, then plan** — commit the docs, then invoke writing-plans to produce
  the S2 implementation plan (PR 1 through PR 7). Recommended: the plan will
  reference these files, so they should exist in history first.
- **Plan without committing** — invoke writing-plans now, commit everything
  together once the plan exists.
- **Commit only** — stop here; write the implementation plan in a later session.
- **Skip the plan** — commit, then implement PR 1 directly against the spec.
